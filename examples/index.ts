/**
 * Memoru examples
 *
 * Run individual examples with:
 * pnpm tsx examples/gc-monitoring.ts
 *
 * GC monitoring example uses PerformanceObserver and works without additional flags.
 */
console.log('Choose an example to run:');
console.log(
  '- gc-monitoring.ts: Demonstrates GC monitoring to prevent cache rotations during garbage collection',
);
